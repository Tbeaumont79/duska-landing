{
  "name": "duska-landing",
  "private": true,
  "type": "module",
  "description": "Landing page marketing publique de Duska — app d'affirmations & citations par humeur.",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "serve": "npx serve .output/public -l 4173",
    "deploy": "nuxt generate && npx -y gh-pages@6 --dotfiles -d .output/public -b gh-pages -m \"deploy: static build [skip ci]\""
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "nuxt": "^3.15.4",
    "typescript": "^5.7.3",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vue-tsc": "^3.3.3"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.10.3",
    "@nuxtjs/robots": "^5.2.2",
    "@nuxtjs/sitemap": "^7.2.4"
  }
}
